spring:
  application:
    name: ms-ticket-manager
  data:
    mongodb:
      uri: mongodb+srv://admin:admin@clusterevent.7ysylg7.mongodb.net/?retryWrites=true&w=majority&appName=ClusterEvent
      database: db_ticket
      auto-index-creation: true

server:
  port: 8081

ms:
  event:
    url: http://localhost:8080/api/v1/events

feign:
  client:
    config:
      default:
        loggerLevel: full

springdoc:
  swagger-ui:
    path: /swagger-ms-ticket-manager.html
  api-docs:
    path: /docs-ms-ticket-manager
  packages-to-scan: com.anamariafelix.ms_ticket_manager.controller